name: Legacy Deploy Disabled

on:
  workflow_dispatch:
    inputs:
      note:
        description: Legacy workflow retained only for reference
        required: false

jobs:
  disabled:
    runs-on: ubuntu-latest

    steps:
      - name: Explain disabled workflow
        run: echo "This workflow is disabled. Publish from dev with npm run deploy instead."
