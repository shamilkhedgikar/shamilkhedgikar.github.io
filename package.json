{
  "name": "shamilkhedgikar.github.io",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "verify": "npm run check && npm run build",
    "prepare:deploy": "node scripts/write-master-readme.mjs",
    "preview": "astro preview",
    "deploy": "npm run build && npm run prepare:deploy && gh-pages -d dist -b master --nojekyll"
  },
  "dependencies": {
    "roughjs": "^4.6.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "@astrojs/mdx": "^4.3.11",
    "@types/node": "^22.19.15",
    "astro": "^5.15.9",
    "gh-pages": "^6.3.0",
    "typescript": "^5.9.3"
  }
}
